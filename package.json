{
  "name": "promptlibrary",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready full-stack scaffold",
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "heroku-postbuild": "node scripts/version-bump.js && npm --prefix client install --include=dev && npm --prefix server install --include=dev && npm --prefix server run prisma:generate && npm --prefix server run heroku-postbuild && npm --prefix server run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
